function msgTime() {
  if (qtdMund.value == "1") {
    alert(
      "O " + iptTim.value + " tem " + qtdMund.value + " Mundial da FIFA"
    );
  } else{
    alert(
      "O " + iptTim.value + " tem " + qtdMund.value + " Mundiais da FIFA"
    );
  }
}
